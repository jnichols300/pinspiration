<%= form_for @pin do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= image_tag(@user.image_url) if @user.image? %>
  <%= f.submit %>
<% end %>
