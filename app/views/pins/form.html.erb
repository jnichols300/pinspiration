<%= form_for @pin do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= image_tag(@user.avatar_url) if @user.avatar? %>
  <%= f.submit %>
<% end %>
